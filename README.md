# Predictive Maintenance Dashboard (Streamlit App)

A data-driven dashboard for predicting industrial equipment failures using IoT sensor data and machine learning.

## Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py
